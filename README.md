# Datalake Project

This repository contains a datalake project scaffold. Use `scripts/create_datalake_structure.py` to (re)create the default folder structure and template files.

Usage:

```bash
python3 scripts/create_datalake_structure.py --root .
```
